import 'package:flutter/material.dart';

class RidersPage extends StatelessWidget {
  const RidersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('RidersPage'),
      ),
      body: const Center(
        child: Text('RidersPage scaffold'),
      ),
    );
  }
}
